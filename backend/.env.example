# Server
PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173,http://localhost:8081

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=consentify_hub

# JWT
JWT_SECRET=srinjoyee_consent_portal_secret_2026
JWT_EXPIRES_IN=24h

# Email (SMTP)
SMTP_HOST=mail.ohctech.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@ohctech.com
SMTP_PASS=
SMTP_FROM=no-reply@ohctech.com

# Admin API access (legacy server-to-server; admin UI uses JWT login)
ADMIN_API_KEY=consent_admin_2026

# Default admin credentials (seeded): admin@wellnesshub.com / Admin@123